[package]
name = "kernel-providers"
version.workspace = true
edition.workspace = true

[dependencies]
kernel-interfaces = { path = "../kernel-interfaces" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "3", features = ["json"] }
